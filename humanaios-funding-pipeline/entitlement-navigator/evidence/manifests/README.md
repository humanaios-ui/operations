# Evidence manifests

This directory contains schemas/examples only. Case-specific manifests, artifact hashes, names, lineage graphs, and raw GEDCOM files belong under the git-ignored `private/` boundary and must not be committed.

Use `genealogy-manifest.example.json` as the shape for a local manifest. A local manifest is provenance metadata, not proof of legal status, title, heirship, or eligibility.
