# humanaios-funding-pipeline requirements
# Pin minor versions for reproducibility; update with `pip install -U`

pydantic>=2.13.4,<3
requests>=2.34.2,<3
beautifulsoup4>=4.15.0,<5
pytest>=9.1.1,<10
lxml>=6.1.1         # faster BS4 parser (optional but recommended)
