# Practice: `humanaios`

**Record:** [`practice.yaml`](./practice.yaml) · **Status:** Z1 CANDIDATE, no Z2 hash
**Zone:** Z-001 (ACTIVE, 100 units/cycle, Z3 executor TBD) — read from `ZONE_REGISTRY.md`
**Resolution class:** **AMBIGUOUS** — read from `ledgers/PRACTICE_RESOLUTION_MAP.md`

## What it owns

Core ACAT instrument development, the behavioral assessment corpus, and the
`acat_corpus_updates` other practices consume. It does not own outreach messaging, mesh
routing, regulatory positioning, or ratification of its own findings.

## Why it is blocked

Four pinned forecast tokens (`T-humanaios-01`..`04`) stand against this practice and **none is
resolved**. Two candidate targets exist — the repository `humanaios-ui/humanaios` and the
scaffold-only mesh directory — and they return different verdicts. Reading the scaffold would
score the tokens NO on a technicality.

This drop adds a third candidate target and resolves nothing. Z2 decision **D1** governs.

## Constraints carried

- Issue #429 §11 — an event is not an ACAT score; the path from event to assessment runs through
  behavioral feature, protocol context, and evidence standing.
- PR #452 Pass A — cross-substrate convergence was observed on the structural-independence gap;
  blindness was not demonstrated.

## Unknowns

Listed in `practice.yaml` under `unknowns`. The largest: whether `humanaios-ui/humanaios` holds
any of the four pinned artifacts. That repository was not read in this session and is outside
this session's repository scope.
