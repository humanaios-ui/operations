# SKILL: Metaculus_main

## 1. Description

**What does this tool do?**  
Runs the Metaculus integration script in `tools/Metaculus/main.py`.

---

## 2. Purpose

**Why does this tool exist?**  
This tool exists to provide a dedicated CLI entry point for Metaculus-related operations within the repository tooling set.

---

## 3. Parameters and Inputs

**What are the required inputs for this tool?**  
Use `--help` to inspect the complete interface for this script.

| Parameter Name | Type | Required | Default Value | Description |
|---|---|---|---|---|
| `--help` | `boolean` | No | `false` | Displays available command options. |

---

## 4. Outputs

**What does this tool return?**  
This tool returns command-line output and may produce files based on selected options.

| Output Name | Type | Description |
|---|---|---|
| `stdout` | `log` | Console output and execution status. |
| `output artifacts` | `file` | Files generated by selected operation flags. |

---

## 5. Usage Example

**How is this tool invoked?**  

```bash
# Show all supported options
python tools/Metaculus/main.py --help
```
