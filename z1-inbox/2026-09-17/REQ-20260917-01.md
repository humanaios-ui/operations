# Agent request REQ-20260917-01 — Wire the decision relay live so the board can rule and request from a browser

**Requested via:** tools/decision_relay.py v0.4.0 (`/task`) · tagline as sent: `Night` (unverified — the HMAC proves the caller knew the secret, not who they are)
**At:** 2026-09-17T20:30:46Z
**Lane:** intent-os
**Wants:** pr
**Status:** OPEN

This is a RECORD, not a candidate: it asks Z2 for nothing and carries no falsifier. A worker — a Claude Code session, a local model behind the relay, or a person — takes it by filling the Fulfilment section in its own PR and citing `REQ-20260917-01` in that PR's body. Governance is unchanged: anything the work needs ratified goes through a candidate block as always.

## Ask

Z2, in session on 2026-09-17: file the first request on the bus and test it, then wire it live to the internet so I can start using INTENT-OS and testing it online.

What is asked: run tools/decision_relay.py on an internet-reachable host with HTTPS, so the local board (ui/intent-os-humanaios-v3_3.html, d17: the board itself stays a local file) can POST /decide, /ratify, /assist and /task from a browser. The relay must keep its own gate (HMAC X-Sig, nonce and epoch) and carry the basic-auth layer the ngrok policy used to provide, so no unauthenticated caller can even probe it. Its GitHub write must be a scoped token set at intake by Z2, never in the tree. The board's project data names the live URL; secrets are entered once per session in the browser and never stored in the file.

Done when: GET on the live URL answers the relay's status JSON behind basic-auth; a signed POST from the board reaches the relay and is answered (a refusal is an answer; a landed record is the proof); the runbook says how to set the two secrets and the token; the request that asked for this carries the PR that did it in its Fulfilment.

This is the beta run: our board, our first request, our own worker.

## Request block

```
REQUEST REQ-20260917-01
  title: Wire the decision relay live so the board can rule and request from a browser
  lane: intent-os
  wants: pr
  tagline: Night (as sent; unverified)
  ask_sha256: 4616700ed04216f92a8673131fbec871356f81f5f92600cb62a7e1b0a8b6fa4a
  at: 2026-09-17T20:30:46Z
```

hash: `7ba140fa73b602d0350a2219f5dabf864331049d1b1d2c7ee86261e7b7698105` — sha256 of the block above; `ask_sha256` inside it is sha256 of the `## Ask` section (lines right-stripped, outer whitespace stripped), so an edited ask breaks the hash.

## Fulfilment

taken_by: Z1 Claude, session_01CePrSjPSB8Epbpq3Lj8oKj (the same session that filed it — Q-INTENTOS-BUS-01's falsifier counts this as one of the ten)
pr:
merged:
at: 2026-09-17T20:31:00Z
