# Candidate Block: Q-INTENTOS-LAUNCH-01 — Intent-OS board: organize the development, decide the launch

**Z1 Proposer:** Claude (AI agent)
**Date Submitted:** 2026-09-14
**Pinned SHA:** `b52f8054ae4756fc8db1fd735e490c346843039a` (main; REGISTERED.md sha256 `8163351e7630e8f5…`, 4096 lines)
**Branch:** `claude/adoring-franklin-rwun0t`
**Phase:** 0/1 (control surface for Z2 decisions)
**Status:** AWAITING Z2 RATIFICATION

---

## §A Position · Destination · Probability

**Position:** `operations` pinned at `b52f805`. The Intent-OS board (`ui/intent-os-humanaios-v3_3.html`)
is Z2's decision surface: 18 pipeline steps, 14 predictions, 16 owner rulings, 23 seals. It was last
read on 2026-09-08 against a commit (`5df54b7`) that no longer exists — the repository history was
reset at `21aec1e` on 09-10 (57 commits total now). It is not a controlled document, not in the tool
manifest's scope, not in the priority queue, and its rulings d2–d16 exist nowhere in
`z1-inbox/INDEX.yaml`, so the z2 gate, the validator and the triage job cannot see them.

**Destination:** a board that (1) runs in a browser and keeps state, (2) can land a ruling through the
relay without the ngrok policy refusing the browser's preflight, (3) can be re-checked against the
tree by a command rather than by memory, (4) has one place where every open owner call lives, and
(5) has a ruled launch surface. (1)–(3) are built in this PR. (4) and (5) are Z2's — d17, d18, d19
below.

**Probability:** **0.55** that by 2026-09-30 the board is reachable at whatever surface d17 names
and `tools/intent_os_board_check_v1_0.py` reports HOLDS on that copy (board prediction C16).
**0.40** that one ruling lands through the relay as a PR rather than by hand in the same window
(C17). Held below 0.60 because both depend on rulings that have a 19-block queue in front of them.

---

## What was found (operated 2026-09-14, all from the tree)

| # | finding | evidence |
|---|---|---|
| 1 | The board's three commit seals are unreachable | `git cat-file -t b8aac1be…`, `c08f86c…`, `5df54b7` → not valid objects; `git rev-list --count HEAD` = 57; root commit `21aec1e` 2026-09-10 |
| 2 | Persistence used an artifact-host API | `window.storage.set/get` in the 09-08 file; undefined in a browser → every tap lost on reload |
| 3 | The relay could not be reached from a browser | `tools/relay_policy.yml` applied basic-auth to every request incl. the CORS preflight; `fetch()` cannot answer a 401 challenge; `decision_relay.py` did not allow the `Authorization` header |
| 4 | `<title>` said v3.1; the file, key and PR history say v3.3 | line 6 |
| 5 | Four "ABSENT" claims are now false, two are path-wrong | `.github/workflows/z2_ratification_gate.yml` present (repaired 09-13 after 122 zero-second runs — Q-GOVGATE-01); `intake_template.jsonl` present at root; `prs_run.py` and `molt_cycle.py` at root, not `tools/`; `tools/benchmark_lint.py` and `criteria/R-001.md` still absent |
| 6 | Registry pin drifted again after Q-IC030-REPIN-01 | 09-10 pin `b35e8036…` at `5d6f081`; today `8163351e…` at `b52f805`; PRIORITY_QUEUE.md row still READY |
| 7 | research-intake was renamed | `specimen-intake.yml:9 formerly: "research-intake (renamed 2026-09-09 … PR #237; Z2 ruling)"` — the board said research-intake in 5 places |
| 8 | Two Z2 backlogs | `z1-inbox/INDEX.yaml`: 22 candidates before this block (19 at b52f805, +3 from #316 merged mid-PR), 3 ratified, 19 awaiting_z2; board: 14 open rulings not in INDEX.yaml; no file on the tree defines GRBS or the GRBS↔Empirica relationship (d5) |
| 9 | Only one static publish path exists | `.github/workflows/pages.yml` deploys `site/**` on push to main; `site/` is generated by `registry_site_generator_v1_0.py`, which leaves other subdirectories alone; `railway.toml` is the ACAT API, not a static host |
| 10 | Empirica wiring is real but thin | `.empirica/project.yaml` (seat `empirica-foundation.carly.empirica-outreach`), `tools/smag_to_empirica_connector.py`, `.breadcrumbs.yaml` tracked despite `.gitignore` (queue hygiene item, still open), NF token `T-empirica-resource-miner-03` = the board's door-3 commitment at p 0.30 for 09-19 |

## What this PR builds (Z1, no Z2 act required)

- **Board re-read** against `b52f805`: 20 steps (two new: 19 "this board", 20 "Z2 decision backlog"), 18 predictions (C1 VOID, C3/C7 resolved FALSE, C8 VOID on its "guard green" clause, C15–C18 new), 19 rulings (d17–d19 new), 40 seals — 33 of them carry a `path:` so a tool can re-hash them.
- **Persistence:** `localStorage` first, the host API as fallback, every access wrapped; the note under the buttons states which store is live.
- **Relay from a browser:** `Authorization: Basic` header sent up front when `relay.basicUser` is set; https required; `relay_policy.yml` exempts `OPTIONS` from basic-auth; `decision_relay.py` allows the `Authorization` header in CORS. Relay self-test still PASS.
- **`tools/intent_os_board_check_v1_0.py`** — re-hashes every seal against the tree; `--self-test` plants every classification (MATCH, DRIFT, MISSING, ABSENT-CONFIRMED, PRESENT, NOT-IN-HISTORY, UNCHECKED) and proves each fires. Registered in `tools-manifest.yaml` by `scan.py`.
- **`docs/INTENT_OS_BOARD_RUNBOOK.md`** — open, read, rule, land, publish, cadence. Front matter `lifecycle: consumed`, consumer = the checker.
- The three lost commits are carried as `RECEIPT-GAP` rows on the board (listed, never counted).

## What Z2 is asked to decide

| id | question | Z1 reading (no preference encoded) |
|---|---|---|
| **d17** | Publish surface — `site/board/` on GitHub Pages (public), or a file Z2 opens locally? | Public is one `cp` + a link and rides `pages.yml`; but the board names an open token exposure (step 3), a PII-in-history question (d8) and every open ruling. Local keeps the relay the only exit. |
| **d18** | Relay landing path — keep `z2-rulings/<date>/` PRs, or land rulings in `z1-inbox/<date>/` + `INDEX.yaml`? | The inbox path is what `.z1-control/ratify.py` already signs and what the z2 gate covers; `z2-rulings/` is outside both. Changing the relay's `land()` is ~15 lines and needs a record entry per ruling. |
| **d19** | Path freeze — keep `ui/intent-os-humanaios-v3_3.html` stable (data changes per read), or bump `v3_x` per read as #218/#221/#224 did? | Two consumer lines already point at the v3_3 path. A bump per read breaks them each time. |
| **row** | Add a priority-queue row for the launch, or leave it as a candidate block? | Per PRIORITY_QUEUE.md §Ratification Authority a new row is a Z2 act. Z1 proposes impact 2, unblocks d-rulings throughput (impact 2) → score 4. |
| **d2–d16** | The fourteen open rulings carried on the board since 09-06/09-08 | Not re-argued here. Once d18 rules, Z1 files one INDEX.yaml candidate per open ruling so they enter the same queue as everything else. |

## Falsifier

If by **2026-09-30** (a) `python3 tools/intent_os_board_check_v1_0.py` still returns STALE against
`main`, or (b) no ruling has landed through the relay (no PR whose body carries a `RULING d<n>` block
and a `hash:` line, in `z2-rulings/` or `z1-inbox/`), then the board is a report and not a control:
retire it to `docs/_archive/` with its sha, and record the rulings d2–d19 directly as INDEX.yaml
candidates without a board. Row-level falsifier for the checker: if a seal with a `path:` changes
on `main` and the checker still reports HOLDS, the tool is broken and must be pulled from the
manifest.

## Receipts (operated this session)

```
python3 tools/intent_os_board_check_v1_0.py --self-test    → SELF-TEST PASS
DRY_RUN=1 python3 tools/decision_relay.py --self-test      → SELF-TEST PASS
python3 molt_cycle.py --read-only --nf ledgers/NF_LEDGER.jsonl → nf_resolved 0 · constants 7
python3 tools/ic_scope_check.py intake_template.jsonl      → REFUSE: no signing secret
python3 tools/doc_lifecycle_lint.py                        → CONSUMED 50 · CODE 6 · ARCHIVE 31 · ARCHIVE? 26 · PRODUCED? 2
node --check <board script>                                → 0
headless Chromium: board renders; checkbox and ruling taps survive reload via localStorage
python3 .tool-control/scan.py && validate.py && render.py  → 154 tools, no violations
```

Attribution: Z1 (Claude) proposes; Z2 (Night) ratifies by hash via `.z1-control/ratify.py`; Z3 lands.
