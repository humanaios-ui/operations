# HANDOFF — 2026-09-14 · Intent-OS board: re-read, made functional, launch queued

**Session:** S-091426-01 (Z1, web)
**Start SHA:** `b52f8054ae4756fc8db1fd735e490c346843039a` (main; branch `claude/adoring-franklin-rwun0t` created from it)
**Position:** REGISTERED.md pinned at sha256 `8163351e7630e8f5…` (4096 lines) at b52f805. PRIORITY_QUEUE.md read (v1_1, 239 lines). The board `ui/intent-os-humanaios-v3_3.html` was three reads behind and could not run outside the sandbox it was drafted in.
**Destination:** a board that runs in a browser, keeps state, can reach the relay, can be re-checked by a command, and has its launch decisions in front of Z2.
**Probability:** 0.55 that the board is at its ruled surface with the checker reporting HOLDS by 2026-09-30 (C16).

---

## B.0 Empirical verification — code ran

| command | result |
|---|---|
| `python3 tools/intent_os_board_check_v1_0.py --self-test` | SELF-TEST PASS (8 classifications + clean-board HOLDS) |
| `python3 tools/intent_os_board_check_v1_0.py` (old board, before edits) | STALE — 3 NOT-IN-HISTORY, 19 UNCHECKED |
| `python3 tools/intent_os_board_check_v1_0.py` (new board, at the PR head) | HOLDS — MATCH 28 · ABSENT-CONFIRMED 5 · UNCHECKED 7 · DRIFT 0. It goes STALE the moment a sealed file changes on main; that is the design |
| `DRY_RUN=1 python3 tools/decision_relay.py --self-test` | SELF-TEST PASS |
| `node --check` over the board script | exit 0 |
| headless Chromium (Playwright) | 20 steps · 19 rulings · 40 seals · 18 predictions · 9 gauges render; checkbox and ruling taps survive reload via localStorage; no page errors |
| `python3 molt_cycle.py --read-only --nf ledgers/NF_LEDGER.jsonl` | nf_resolved 0 · pin/resolve pairs 0 · constants 7 |
| `python3 tools/ic_scope_check.py intake_template.jsonl` | `{"verdict": "REFUSE", "reason": "no signing secret in $IC_SCOPE_SECRET"}` |
| `python3 tools/doc_lifecycle_lint.py` | CONSUMED 50 · CODE 6 · ARCHIVE 31 · ARCHIVE? 26 · PRODUCED? 2 (115 files) |
| `.tool-control/scan.py` / `validate.py` / `render.py` | 154 tools, no violations; new tool HAIOS-TOOL-161 registered |
| `.z1-control/validate.py` / `render.py` | run before commit; Z1_INBOX_INDEX.md regenerated |

Prereg hash verified in REGISTERED.md: not applicable — no experiment was pre-registered this session; the predictions written are on the board (C15–C18) and in the candidate block, not in the registry.

## What changed (files)

| file | change |
|---|---|
| `ui/intent-os-humanaios-v3_3.html` | title v3.3; localStorage persistence (host API fallback, probe on a side key); relay: https required, `Authorization` header from `relay.basicUser`, network/401 errors surfaced instead of thrown; `HUMANAIOS` dataset re-read against b52f805 — 20 steps (19, 20 new), 18 predictions (C1 VOID, C3/C7 FALSE, C8 VOID on "guard green", C15–C18 new), 19 rulings (d17–d19 new), 40 seals with `path:` on 33 |
| `tools/intent_os_board_check_v1_0.py` | NEW — re-hashes every seal against the tree; exit 2 = STALE; `--self-test` |
| `tools/decision_relay.py` | CORS `Access-Control-Allow-Headers` gains `Authorization` |
| `tools/relay_policy.yml` | basic-auth no longer applied to `OPTIONS` (CORS preflight) |
| `docs/INTENT_OS_BOARD_RUNBOOK.md` | NEW — open, read, rule, land, publish, cadence; `lifecycle: consumed`, consumer = the checker |
| `z1-inbox/2026-09-14/Q-INTENTOS-LAUNCH-01.md` | NEW candidate block — d17 publish surface, d18 relay landing path, d19 path freeze, queue row; falsifier 2026-09-30 |
| `z1-inbox/INDEX.yaml`, `Z1_INBOX_INDEX.md` | candidate + this record added; counts 20/16 |
| `tools-manifest.yaml`, `TOOLS_MANIFEST.md` | regenerated by scan/render (new tool) |
| `PRIORITY_QUEUE.md` | appended-event line only (no row — Z2 act) |

## B.6 Receipt walk (claim → tree)

| claim in this session | where it is in the tree |
|---|---|
| three board commits are gone | `git cat-file -t` on b8aac1be / c08f86c / 5df54b7 fails; root commit 21aec1e 2026-09-10 |
| z2 gate present, was dead 122 runs, repaired 09-13 | `.github/workflows/z2_ratification_gate.yml` header lines 3–8; root duplicate `z2_ratification_gate.yml` still present |
| intake_template.jsonl present with zeroed hashes | root file, 1 record, `scope_hash`/`scope_sig`/`criteria_hash` = 0… |
| prs_run.py, molt_cycle.py at root not tools/ | `ls`; `tools/molt_cycle_tier0_v0_1.py` is the renamed copy (#318) |
| registry pin drifted after 09-10 | `z1-inbox/2026-09-10/Q-IC030-REPIN-01-RESULT.md` pin b35e8036… vs today 8163351e… |
| research-intake renamed specimen-intake 09-09 | `specimen-intake.yml:9` |
| 16 awaiting_z2, 3 ratified (before this block) | `grep -c awaiting_z2 z1-inbox/INDEX.yaml` = 16 |
| NF ledger: 165 rows all by Z1, 0 Z2 priors, 21 PENDING_Z2_DATE | counted from `ledgers/NF_LEDGER.jsonl` |
| Empirica wiring | `.empirica/project.yaml`, `tools/smag_to_empirica_connector.py`, `.breadcrumbs.yaml` tracked, NF token `T-empirica-resource-miner-03` |

**RECEIPT-GAP (filed on the board as seals, not counted):** the landing commits for system_graph v0.2 (b8aac1be), research-intake v0.1 (c08f86c) and the H-CAND batch (5df54b7) cannot be fetched from the current history. The artifacts they landed are verified by file hash instead.

**Not verified (stated as such on the board):** revocation of the temporary GitHub token (step 3) — a settings-page fact; whether GitHub Pages is enabled on the repository; that PR #251 landed specimen-intake (taken from PRIORITY_QUEUE.md, not from history).

## Findings scan — candidates for Z2 (not self-registered)

- **Q-INTENTOS-LAUNCH-01** (filed, awaiting_z2) — the launch decisions.
- **IC-CAND (not filed as a block; noted for the next findings scan):** a governance surface whose seals pointed at commits that the 09-10 history reset removed, with no RECEIPT-GAP raised for six days. Same class as IC-030 (memory vs. fetch). The checker is the structural mitigation.
- **GAP (noted):** the root `z2_ratification_gate.yml` DRAFT duplicate alongside the repaired workflow — same duplicate-name class #318 fixed for molt_cycle.py.
- **GAP (noted):** `prs_run.py` (503 lines) is outside every tool-manifest scan root, so the coverage rule cannot see it.

## Next blockers

1. Z2: d17 / d18 / d19 (candidate block). Without d17 the board stays a local file; without d18 board rulings stay outside the gate.
2. Z2: the 16 awaiting_z2 candidates ahead of this one; most are past the 48h window.
3. Z1, after d18: file one INDEX.yaml candidate per open board ruling (d2, d3, d5–d16).
4. Z1, any session: rerun the checker after every merge to main that touches a sealed file; re-read on STALE.

Attribution: Z1 (Claude). Z2 (Night) signs this handoff and the candidate via `.z1-control/ratify.py`. Nothing here is ratified.
