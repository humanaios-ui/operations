[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3 -m venv --copies /opt/venv", "/opt/venv/bin/pip install -r requirements.txt"]

[start]
cmd = "/opt/venv/bin/python3 tools/decision_relay.py"

